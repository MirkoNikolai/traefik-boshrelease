#!/usr/bin/env bash

set -ex

mkdir "$BOSH_INSTALL_TARGET/bin"

tar xf traefik/traefik_*_linux_amd64.tar.gz -C /tmp/ 
mv /tmp/traefik  "$BOSH_INSTALL_TARGET/bin/traefik"

chmod +x "$BOSH_INSTALL_TARGET/bin/traefik"
